apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-static-web.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "simple-static-web.name" . }}

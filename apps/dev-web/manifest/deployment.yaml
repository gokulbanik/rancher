apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-web
  namespace: dev-web 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dev-web
  template:
    metadata:
      labels:
        app: dev-web
    spec:
      containers:
      - name: dev-web
        image: simple-static-web:1.0.1
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/conf.d/default.conf
          subPath: default.conf
          readOnly: true
        - name: html
          mountPath: /usr/share/nginx/html
        - name: auth
          mountPath: /etc/nginx/auth/auth
          subPath: auth
          readOnly: true
      volumes:
      - name: nginx-config
        configMap:
          name: dev-nginx-config
      - name: html
        configMap:
          name: dev-html
      - name: auth
        secret:
          secretName: basic-auth-secret-dev
